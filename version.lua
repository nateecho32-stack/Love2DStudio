-- Single source of truth for the studio version. Bump on releases; the
-- window title and the console `version` command read it.
return {
  major = 0,
  minor = 2,
  patch = 0,
  string = "0.2.0",
}
